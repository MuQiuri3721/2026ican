# 森林火灾救援智能体项目方案

## 一、项目概述

### 1.1 项目背景

原方案需要制作实体无人机并完成仿真，开发周期较长，难以满足创新赛道的提交要求。经团队讨论，项目转投挑战赛道，采用软件系统、智能体应用和演示视频作为主要成果。

项目截止时间为 **2026 年 9 月 30 日**。

### 1.2 项目定位

本项目面向森林火灾救援场景，构建一个基于多源数据融合和智能体协同的无人机资源调度系统。系统通过视觉识别、环境数据分析、无人机状态管理和规则计算，生成火灾救援方案。

项目最终不要求真实控制无人机，而是通过可运行的软件系统和录屏视频，展示从火灾图像输入到救援决策输出的完整过程。

### 1.3 最终效果

用户上传无人机拍摄的火灾图片或视频截图后，系统完成以下任务：

- 识别火点、火焰范围和烟雾范围；
- 判断火情等级和火势发展趋势；
- 获取固定森林区域的地形、海拔、水源和气象数据；
- 获取无人机的电量、位置、载荷和任务状态；
- 判断是否能够控制当前火情；
- 计算所需无人机数量和灭火资源；
- 分配侦察、灭火、补给和返航任务；
- 输出可解释的救援调度方案。

---

## 二、最终系统方案

### 2.1 系统总体流程

```text
上传火灾图片或视频
        ↓
YOLO 识别火点、火焰范围和烟雾
        ↓
读取固定区域环境数据
        ↓
读取无人机和物资状态
        ↓
智能体综合分析火情
        ↓
规则引擎计算资源和任务
        ↓
输出灭火、补给、返航和监测方案
        ↓
根据新的监测结果进行再次决策
```

系统形成“感知—研判—调度—执行—反馈—再决策”的闭环。

### 2.2 无人机集群分工

| 无人机 | 主要职责 |
|---|---|
| 无人机一 | 火情侦察、图像采集、火势监测和结果回传 |
| 无人机二 | 携带水、干粉等灭火资源，执行主要灭火任务 |
| 无人机三 | 物资补给、能源支援、后备保障和人群疏散 |

无人机三主要承担支援任务，不直接承担主要灭火任务。人群疏散功能属于扩展能力，可以在后续版本或演示视频中体现。

### 2.3 输入数据

#### 火情数据

- 火点位置；
- 火焰面积；
- 烟雾面积；
- 火情等级；
- 火势变化趋势。

#### 环境与地理数据

- 风速和风向；
- 海拔高度；
- 地形地貌；
- 水源位置和距离；
- 森林区域边界；
- 烟雾可能扩散方向。

#### 资源数据

- 无人机位置；
- 无人机电量；
- 无人机载荷；
- 无人机飞行速度；
- 无人机当前任务状态；
- 水、干粉等灭火物资库存。

### 2.4 输出结果

系统至少输出以下内容：

- 当前火情等级；
- 火势范围和可能蔓延方向；
- 是否可以控制火情；
- 建议出动的无人机数量；
- 每架无人机的任务；
- 所需灭火资源类型和数量；
- 是否需要就近取水；
- 是否需要无人机三补给；
- 是否需要增援；
- 是否需要返航充电；
- 下一次监测和评估时间。

---

## 三、技术架构

### 3.1 总体架构

项目采用“视觉识别 + LLM/VLM 分析 + 规则引擎 + Tool 调用”的架构。

```text
Vue 前端
  ↓ HTTP
FastAPI 后端
  ↓
智能体工作流
  ├─ 火情感知模块
  ├─ 环境研判模块
  ├─ 资源调度模块
  └─ 闭环监测模块
  ↓
Tool 集合
  ├─ YOLO 火点检测
  ├─ 地理信息查询
  ├─ 无人机状态查询
  ├─ 火情等级判断
  ├─ 资源计算
  ├─ 任务分配
  ├─ 补给模拟
  └─ 返航模拟
```

### 3.2 多智能体的实现方式

为了控制开发难度，不建议一开始开发多个独立服务。可以使用一个智能体工作流，内部划分多个逻辑角色：

1. **火情感知 Agent**：调用 YOLO，识别火点和火焰范围。
2. **环境研判 Agent**：读取地理和气象数据，分析火势蔓延条件。
3. **资源调度 Agent**：结合无人机状态和物资库存，生成调度方案。
4. **闭环监测 Agent**：根据新的监测结果，决定继续灭火、补给、增援或返航。

这些角色可以作为 LangGraph 节点或普通 Python 函数实现，不要求每个角色单独部署模型。

### 3.3 LLM 与规则引擎的职责

LLM/VLM 负责：

- 理解图片和结构化数据；
- 解释火情；
- 分析复杂环境因素；
- 编排 Tool 的调用顺序；
- 生成自然语言形式的决策说明。

规则引擎负责：

- 火情等级划分；
- 无人机数量计算；
- 灭火剂用量计算；
- 电量和载荷校验；
- 返航和补给条件判断；
- 安全约束和结果校验。

关键数量和安全条件不应完全交给大模型计算。

---

## 四、按照 m 的方案实现

### 4.1 实现原则

m 提出的核心方案是：

> 先用假数据跑通“输入—分析—输出”流程，再逐步替换为真实模型和真实数据。

开发过程中遵循以下原则：

1. 先完成简单、可验证的功能；
2. 将功能拆分为独立 Tool 或 Skill；
3. 前一个 Tool 的输出作为后一个 Tool 的输入；
4. 每完成一个模块就进行测试；
5. 在第一版流程跑通后，再增加复杂功能；
6. 会议文档、代码和演示材料同步维护。

### 4.2 基础数据结构

各模块统一使用结构化数据，避免直接传递自然语言：

```json
{
  "fire": {
    "level": 2,
    "area_m2": 1800,
    "smoke_area_m2": 4200,
    "location": [118.78, 32.04]
  },
  "environment": {
    "wind_speed": 6.5,
    "wind_direction": "西北",
    "altitude": 320,
    "water_source_distance": 800,
    "terrain": "丘陵"
  },
  "fleet": [
    {
      "id": "drone-1",
      "role": "reconnaissance",
      "battery": 82,
      "payload": 0
    },
    {
      "id": "drone-2",
      "role": "firefighting",
      "battery": 75,
      "payload": 80
    },
    {
      "id": "drone-3",
      "role": "support",
      "battery": 91,
      "payload": 120
    }
  ]
}
```

### 4.3 基础 Tool

建议按以下顺序实现：

| Tool | 功能 |
|---|---|
| `detect_fire` | 调用 YOLO 检测火点和火焰范围 |
| `get_environment` | 查询固定森林区域的环境数据 |
| `get_fleet_status` | 查询无人机电量、位置和载荷 |
| `assess_fire` | 判断火情等级和火势状态 |
| `calculate_resources` | 计算灭火资源和无人机数量 |
| `plan_dispatch` | 生成无人机任务分配方案 |
| `resupply` | 模拟物资补给或补水 |
| `return_to_charge` | 模拟返航和充电 |
| `evaluate_result` | 评估灭火效果并触发下一轮决策 |

### 4.4 第一版工作流

```text
输入图片
  ↓
detect_fire
  ↓
get_environment
  ↓
get_fleet_status
  ↓
assess_fire
  ↓
calculate_resources
  ↓
plan_dispatch
  ↓
输出救援方案
```

第一版不需要复杂自主规划，只要流程稳定、输出合理即可。后续再增加补给、返航、持续监测和增援决策。

---

## 五、最小可运行 Demo

### 5.1 Demo 场景

固定一个森林区域，准备以下数据：

- 一张火灾图片；
- 一份火点位置数据；
- 一份地形、海拔和水源数据；
- 一组风向和风速数据；
- 三类无人机的初始状态；
- 一组物资库存数据。

### 5.2 用户操作

1. 打开系统网页；
2. 上传火灾图片；
3. 点击“开始研判”；
4. 查看 YOLO 火点检测结果；
5. 查看环境和无人机状态；
6. 查看智能体分析过程；
7. 查看最终调度方案。

### 5.3 Demo 输出示例

```text
火情等级：Ⅱ级，中等火情
火焰面积：约 1800 平方米
烟雾面积：约 4200 平方米
主要蔓延方向：西北方向
处置结论：当前火情可以控制

无人机一：持续侦察并回传图像
无人机二：携带 80 kg 水剂执行灭火
无人机三：前往水源点补给，并作为后备支援
预计处置时间：18 分钟
建议：每 5 分钟重新评估一次火势
```

### 5.4 MVP 暂不实现

- 真实无人机飞控；
- 实体设备控制；
- 自行训练 YOLO 模型；
- 复杂三维地图和真实避障；
- 真实传感器实时接入；
- 复杂人群疏散系统；
- 多个独立大模型服务同时协作。

上述内容可以作为 PDF 中的应用前景和视频中的扩展设想。

---

## 六、项目模块拆分

```text
forest-fire-agent/
├─ backend/
│  ├─ api/              # 文件上传、启动分析、查询结果
│  ├─ agents/           # 智能体工作流
│  ├─ tools/            # YOLO、环境查询、调度和补给工具
│  ├─ domain/           # 火情、无人机、任务和方案数据结构
│  ├─ rules/            # 火情等级和资源计算规则
│  ├─ adapters/         # YOLO、Kimi、千问等模型适配器
│  └─ data/             # 固定区域和无人机假数据
├─ frontend/
│  ├─ upload/           # 图片或视频上传
│  ├─ analysis/         # 火情分析结果
│  ├─ dispatch/         # 无人机调度结果
│  └─ monitor/          # 任务执行和反馈
├─ docs/
│  ├─ requirements.md   # 需求说明
│  ├─ architecture.md   # 系统架构
│  └─ demo-script.md    # 演示脚本
└─ README.md
```

---

## 七、技术栈建议

| 部分 | 推荐技术 |
|---|---|
| 后端 | Python 3.11 + FastAPI |
| 数据校验 | Pydantic |
| 智能体编排 | LangGraph，或先使用普通 Python 工作流 |
| 视觉识别 | Ultralytics YOLO + OpenCV |
| 大模型 | Kimi / 千问，通过统一适配器调用 |
| 前端 | Vue 3 + Vite |
| 数据存储 | MVP 使用 JSON 或 SQLite |
| 地理数据 | GeoJSON；固定场景暂不引入复杂 GIS 服务 |
| RAG | 初版使用本地 JSON 检索，后续再接 FAISS 或 Chroma |
| 接口文档 | FastAPI Swagger |
| 代码协作 | GitHub + Git |
| 演示制作 | 浏览器录屏 |

第一版不建议引入 PostgreSQL、Redis、Docker、复杂 GIS 服务和向量数据库，以免影响核心流程开发。

---

## 八、成员分工

### 江月

- 整理会议记录；
- 撰写 Word 长文档和 PDF 方案；
- 负责总体需求和项目协调；
- 细化无人机数量、位置、速度、载荷和耗电参数；
- 负责最终报告、演示脚本和进度推进。

### m（马其瑞）

- 建立 GitHub 仓库；
- 搭建 FastAPI 和前端基础框架；
- 梳理整体技术栈；
- 定义模块接口和统一数据格式；
- 使用假数据跑通“输入—分析—输出”流程；
- 负责智能体工作流整合。

### 听日

- 确认 YOLO Best 模型权重；
- 跑通火点检测和定位；
- 整理模型输入输出格式；
- 确认数据集来源、许可证和拍摄地点信息。

### 杨涵

- 调研地形、地貌、海拔和水源数据；
- 整理 GeoJSON 或 JSON 数据；
- 实现环境数据查询；
- 为调度模块提供环境输入。

### 吉相如

- 负责 VLM/LLM 接入；
- 测试 Kimi、千问等视觉模型；
- 设计火情分析 Prompt；
- 将模型输出转换为统一 JSON 格式。

---

## 九、开发计划

### 第一阶段：9 月 3 日至 9 月 7 日

- 建立 GitHub 仓库；
- 确定技术栈；
- 定义统一数据结构；
- 使用假数据跑通基础流程。

### 第二阶段：9 月 8 日至 9 月 14 日

- 跑通 YOLO；
- 接入固定森林区域数据；
- 完成火情等级判断；
- 完成无人机参数和资源计算规则。

### 第三阶段：9 月 15 日至 9 月 21 日

- 接入 LLM/VLM；
- 完成智能体 Tool 调用；
- 生成无人机调度方案；
- 完成前端结果展示。

### 第四阶段：9 月 22 日至 9 月 26 日

- 增加补给、返航和闭环监测模拟；
- 联调全部模块；
- 修复演示流程问题；
- 开始录制视频和撰写 PDF。

### 第五阶段：9 月 27 日至 9 月 29 日

- 完成最终测试；
- 整理代码和 README；
- 完成 PDF、视频和可运行程序；
- 预留时间处理突发问题。

---

## 十、项目核心原则

项目重点不是实现真实无人机控制，而是清晰展示以下技术链路：

> 多源数据如何进入系统，智能体如何调用 Tool，规则引擎如何计算资源，最终如何生成可解释的救援调度方案。

只要最小 Demo 能够稳定完成这条链路，项目就具备完整的技术逻辑、展示效果和方案价值。

---

## 十一、Skill、Tool 与数据流设计

### 11.1 Skill 和 Tool 的区别

- **Tool**：一个可独立调用的原子能力，输入和输出明确，尽量由确定性代码完成。例如“查询无人机电量”“计算所需水量”“规划航线”。
- **Skill**：一个面向业务目标的完整能力，由多个 Tool 按顺序组合而成。例如“火情研判 Skill”会组合火点检测、环境查询和火情等级判断。
- **Agent**：负责理解任务、组织 Skill、决定调用顺序并解释结果。

建议采用以下关系：

```text
Agent
  ↓ 组织
Skill
  ↓ 调用
Tool
  ↓ 读取或生成
结构化数据
```

### 11.2 Skill 总体拆分

| Skill | 业务目标 | 主要输入 | 主要输出 | 依赖 Tool |
|---|---|---|---|---|
| 火情感知 Skill | 从图片或视频中提取火情 | 图片、视频、场景信息 | 火点位置、火焰面积、烟雾面积、置信度 | `extract_frames`、`detect_fire`、`calculate_fire_metrics` |
| 环境研判 Skill | 分析火势发展的环境条件 | 火点位置、风速风向、地形、海拔、水源 | 蔓延方向、可用水源、环境风险 | `get_environment`、`calculate_wind_vector`、`predict_spread` |
| 火情评估 Skill | 判断火情等级和处置难度 | 火情数据、环境数据 | 火情等级、风险分数、预计处置难度 | `assess_fire_level`、`estimate_growth` |
| 资源匹配 Skill | 决定使用何种灭火资源 | 火情等级、水源、库存、火灾类型 | 资源类型、资源总量、补给需求 | `match_extinguisher`、`calculate_resource_need`、`get_inventory` |
| 无人机调度 Skill | 计算无人机数量和任务 | 火情、环境、资源、机群状态 | 无人机任务分配、执行顺序 | `get_fleet_status`、`calculate_drone_count`、`assign_tasks` |
| 航线规划 Skill | 生成从当前位置到任务点的路线 | 起点、终点、地形、障碍和风险 | 航线、距离、预计时间、风险值 | `calculate_distance`、`plan_route` |
| 任务执行 Skill | 模拟灭火、补给和返航 | 调度计划、无人机状态、任务时长 | 更新后的状态、消耗量、任务日志 | `execute_firefighting`、`resupply`、`return_to_charge` |
| 闭环监测 Skill | 根据新数据重新决策 | 新一轮图像、火势、无人机和物资状态 | 继续、增援、补给、返航或结束 | `evaluate_result`、`update_fire_state`、`make_next_decision` |
| 人群疏散 Skill | 为人员密集场景提供疏散方案 | 人员位置、火点、烟雾、道路和风向 | 疏散区域、路线和语音提示 | `get_population_data`、`plan_evacuation_route`、`generate_voice_notice` |

人群疏散 Skill 属于扩展功能，不应阻塞森林火灾核心 Demo。

### 11.3 Tool 详细清单

#### A. 输入与视觉识别类 Tool

| Tool | 输入 | 输出 | 实现方式 |
|---|---|---|---|
| `extract_frames` | 视频文件、抽帧间隔 | 图片帧列表、时间戳 | OpenCV |
| `detect_fire` | 单张图片 | 检测框、类别、置信度、像素坐标 | YOLO |
| `calculate_fire_metrics` | 检测框、图片尺寸、场景比例 | 火焰面积、火点中心、烟雾面积 | Python 计算 |
| `analyze_visual_trend` | 多帧检测结果 | 面积变化率、位置变化、趋势 | OpenCV/统计计算 |
| `analyze_with_vlm` | 图片或图片序列、结构化检测结果 | 火情描述、异常说明、补充判断 | Kimi/千问等 VLM |

VLM 可以辅助解释图像和多帧变化，但风速、风向等精确数值应优先来自环境数据，而不是完全依赖图片推断。

#### B. 环境与地理信息类 Tool

| Tool | 输入 | 输出 | 实现方式 |
|---|---|---|---|
| `get_environment` | `scene_id`、坐标 | 地形、海拔、水源、风速、风向 | JSON/GeoJSON/SQLite |
| `get_water_sources` | 火点坐标、最大搜索距离 | 水源列表、距离、可用性 | 空间距离查询 |
| `calculate_wind_vector` | 风速、风向角 | 二维风向量 | 三角函数 |
| `predict_spread` | 火点、风向量、坡度、时间 | 预测蔓延中心、方向和范围 | 简化传播模型 |
| `retrieve_scene_knowledge` | 场景 ID、关键词 | 相关地理知识片段 | 本地 JSON 检索/RAG |

#### C. 火情和资源计算类 Tool

| Tool | 输入 | 输出 | 实现方式 |
|---|---|---|---|
| `assess_fire_level` | 火焰面积、烟雾面积、风速、增长率 | 火情等级、风险分数 | 加权评分规则 |
| `estimate_growth` | 当前面积、风速、地形、时间 | 未来火焰面积 | 增长模型 |
| `match_extinguisher` | 火情类型、水源和库存 | 推荐水、干粉或其他资源 | 规则表 |
| `calculate_resource_need` | 火焰面积、火情等级、资源类型 | 总资源需求、单位时间需求 | 资源消耗模型 |
| `get_inventory` | 无 | 水、干粉和备用物资库存 | JSON/SQLite |
| `validate_plan` | 调度方案、约束条件 | 是否可执行、违规原因 | 规则校验 |

#### D. 无人机和调度类 Tool

| Tool | 输入 | 输出 | 实现方式 |
|---|---|---|---|
| `get_fleet_status` | 无或机群 ID | 电量、位置、载荷、速度、状态 | JSON/SQLite |
| `calculate_drone_count` | 总需求、单机能力、可用机群 | 建议无人机数量 | 向上取整和约束校验 |
| `assign_tasks` | 无人机列表、任务列表、路线 | 每架无人机的任务 | 贪心分配/优化算法 |
| `calculate_distance` | 起点、终点 | 距离和预计飞行时间 | 平面距离或 GIS 距离 |
| `plan_route` | 起点、终点、地形和障碍 | 路径点、总距离、风险 | MVP 直线距离，后续 A* |
| `check_battery` | 无人机状态、路线、任务时长 | 是否满足电量约束 | 能耗模型 |
| `check_payload` | 无人机载荷、物料需求 | 是否满足载荷约束 | 载荷校验 |

#### E. 执行和闭环类 Tool

| Tool | 输入 | 输出 | 实现方式 |
|---|---|---|---|
| `execute_firefighting` | 无人机、资源、任务时长 | 火情面积变化、资源消耗 | 模拟计算 |
| `resupply` | 支援无人机、目标无人机、物资 | 补给后的状态 | 状态更新 |
| `return_to_charge` | 无人机、返航点 | 返航时间、电量变化、状态 | 状态更新 |
| `update_fire_state` | 当前火情、灭火效果、时间 | 下一时刻火情状态 | 状态方程 |
| `evaluate_result` | 前后两轮火情数据 | 效果评分、是否达标 | 指标计算 |
| `make_next_decision` | 效果、资源和电量状态 | 继续、增援、补给、返航或结束 | 决策规则 |

## 十二、模块之间的依赖关系

### 12.1 主链路

```text
图片/视频
  ↓
火情感知 Skill
  ├─ extract_frames
  ├─ detect_fire
  ├─ calculate_fire_metrics
  └─ analyze_visual_trend
  ↓ 火情结构化数据
环境研判 Skill
  ├─ get_environment
  ├─ get_water_sources
  ├─ calculate_wind_vector
  └─ predict_spread
  ↓ 火情 + 环境数据
火情评估 Skill
  ├─ assess_fire_level
  └─ estimate_growth
  ↓ 火情等级和处置需求
资源匹配 Skill
  ├─ match_extinguisher
  ├─ get_inventory
  └─ calculate_resource_need
  ↓ 资源需求
调度 Skill
  ├─ get_fleet_status
  ├─ calculate_drone_count
  ├─ plan_route
  ├─ check_battery
  ├─ check_payload
  └─ assign_tasks
  ↓ 调度计划
任务执行与闭环 Skill
  ├─ execute_firefighting
  ├─ resupply
  ├─ return_to_charge
  ├─ update_fire_state
  └─ make_next_decision
```

### 12.2 数据依赖关系

| 上游输出 | 下游使用位置 |
|---|---|
| YOLO 火焰面积和位置 | 火情等级、资源需求、路线起点 |
| 烟雾面积和多帧增长率 | 火情趋势、风险评分、疏散预警 |
| 风速和风向 | 蔓延预测、航线风险、疏散方向 |
| 海拔和地形 | 悬停位置、航线成本、火势传播修正 |
| 水源位置和距离 | 灭火资源选择、补水任务、路线规划 |
| 无人机电量和位置 | 可用无人机筛选、返航判断、任务分配 |
| 无人机载荷和物资库存 | 灭火剂分配、补给决策 |
| 本轮灭火效果 | 下一轮火情预测和闭环决策 |

## 十三、数学逻辑设计

以下模型用于软件演示和调度逻辑验证，参数属于项目假设，不代表真实消防作业标准。所有参数应集中放在配置文件中，方便后续修改。

### 13.1 图像面积换算

YOLO 先输出火焰检测框或分割区域。设火焰区域像素面积为 `P_fire`，单个像素对应的实际长度为 `g` 米，则实际面积为：

```text
A_fire = P_fire × g²
```

如果没有真实相机标定数据，MVP 可以使用固定场景比例：

```text
A_fire = P_fire × area_scale
```

其中 `area_scale` 由固定演示图片人工标定。视频中必须注明这是演示估算值。

### 13.2 火情等级评分

将不同指标归一化到 0 到 1：

```text
a = min(A_fire / A_ref, 1)
s = min(A_smoke / S_ref, 1)
w = min(v_wind / V_ref, 1)
g = min(growth_rate / G_ref, 1)
```

定义火情风险分数：

```text
R_fire = αa + βs + γw + δg
α + β + γ + δ = 1
```

示例分级规则：

```text
R_fire < 0.30       → I 级，低风险
0.30 ≤ R_fire < 0.60 → II 级，中风险
0.60 ≤ R_fire < 0.80 → III 级，高风险
R_fire ≥ 0.80       → IV 级，极高风险
```

`α、β、γ、δ` 和分级阈值只用于项目模拟，应在配置文件中维护。实际系统需要真实数据和专业标准重新标定。

### 13.3 风向量和火势蔓延

将风速 `v` 和风向角 `θ` 转换为二维向量：

```text
V_wind = (v × cos θ, v × sin θ)
```

为了体现会议中讨论的风向、风速和地形影响，可以使用简化传播模型：

```text
V_spread = k_w × V_wind + k_s × ∇H + k_t × V_terrain
```

其中：

- `V_spread`：预测火势蔓延方向和速度；
- `V_wind`：风向量；
- `∇H`：地形高程梯度，表示坡度方向；
- `V_terrain`：地形或植被影响向量；
- `k_w、k_s、k_t`：可调权重。

预测时间 `Δt` 后的火势中心：

```text
p_next = p_current + V_spread × Δt
```

MVP 可以只使用风向量；有地形数据后，再加入坡度修正。VLM 对多帧图片的判断可以作为趋势参考，但不应替代风速传感器或结构化气象数据。

### 13.4 火势面积增长模型

设当前火焰面积为 `A_t`，自然增长率为 `r_growth`，风速修正系数为 `k_wind`，时间步长为 `Δt`：

```text
r_effective = r_growth × (1 + k_wind × v_wind)
A_before_control = A_t × (1 + r_effective × Δt)
```

这是线性近似模型，主要用于演示“风速越大，火势增长越快”的逻辑。

### 13.5 灭火资源需求

设单位面积基础灭火资源需求为 `q_base`，火情等级修正系数为 `k_level`，环境风险修正系数为 `k_env`，安全系数为 `k_safe`：

```text
M_required = A_fire × q_base × k_level × k_env × k_safe
```

单位时间所需灭火流量为：

```text
Q_required = M_required / T_target
```

其中：

- `M_required`：总灭火资源需求；
- `Q_required`：单位时间资源需求；
- `T_target`：目标处置时间；
- `q_base`、`k_level`、`k_env`、`k_safe`：项目配置参数。

水源足够且距离较近时，推荐“就近取水”；水源不足时，可切换为预装水或干粉，并由资源匹配 Tool 返回理由。

### 13.6 无人机数量计算

对每架可用灭火无人机 `i`，设其有效灭火流量为 `q_i`。需要满足：

```text
Σ q_i × x_i ≥ Q_required
```

其中 `x_i` 表示是否选中无人机 `i`。最简单的 MVP 算法为按有效流量从高到低排序，依次选择无人机，直到满足需求：

```text
n = ceil(Q_required / Q_single)
```

当无人机能力不同或电量有限时，使用：

```text
Σ payload_i × x_i ≥ M_required
Σ battery_i × x_i ≥ E_required
x_i ∈ {0, 1}
```

同时检查每架无人机的角色、载荷和最低剩余电量。

### 13.7 飞行时间和电量约束

对无人机 `i`，起点到任务点距离为 `d_out`，返航距离为 `d_back`，飞行速度为 `v_i`，悬停和执行任务时间为 `T_task`：

```text
T_flight = (d_out + d_back) / v_i
T_total = T_flight + T_task
E_required = c_i × T_total
```

只有满足以下条件，才允许分配任务：

```text
E_required ≤ E_available - E_reserve
payload_i ≥ payload_required
```

若不满足，则触发以下决策之一：

- 更换距离更近的无人机；
- 减少单架无人机任务量；
- 安排无人机三补给；
- 先返航充电；
- 请求增援。

### 13.8 航线规划

MVP 阶段使用平面距离：

```text
d = √((x₂ - x₁)² + (y₂ - y₁)²)
T = d / v
```

后续将地图划分为网格，每个网格设置通行成本：

```text
C_cell = d_cell × (1 + λ_slope × slope + λ_risk × risk + λ_obstacle × obstacle)
```

使用 A* 搜索总成本最低的路径。这样可以把山地、火场高风险区域和不可通行区域纳入航线规划。

### 13.9 闭环状态更新

设当前火焰面积为 `A_t`，自然增长面积为 `G_t`，无人机产生的有效灭火量为 `F_t`：

```text
A_next = max(0, A_t + G_t - F_t)
```

其中：

```text
G_t = A_t × r_effective × Δt
F_t = k_ext × Q_total × Δt
```

`k_ext` 表示灭火资源转化为火焰面积减少量的效率系数。

每轮监测后按照以下逻辑判断：

```text
如果 A_next ≤ A_target：结束任务，生成效果报告
如果电量不足：安排返航或充电
如果物料不足：调用无人机三补给
如果 A_next > A_t：请求增援
否则：继续灭火并进入下一轮监测
```

## 十四、调度决策的数学关系

### 14.1 目标函数

在后续版本中，可以将调度问题写成一个带约束的优化问题：

```text
minimize J = α × T_total + β × D_total + γ × Risk_total + δ × Resource_cost
```

其中：

- `T_total`：总任务时间；
- `D_total`：无人机总飞行距离；
- `Risk_total`：航线和火场风险；
- `Resource_cost`：水、干粉和补给成本；
- `α、β、γ、δ`：项目偏好权重。

约束条件包括：

```text
灭火总量 ≥ 火情所需资源
每架无人机任务量 ≤ 载荷上限
任务耗电量 ≤ 可用电量 - 安全余量
无人机角色必须与任务类型匹配
路线不能经过不可通行区域
支援时间必须早于主力无人机资源耗尽时间
```

### 14.2 MVP 采用的算法

不建议第一版直接实现复杂优化算法。推荐使用以下确定性流程：

1. 根据风险分数判断火情等级；
2. 根据火情等级计算总资源需求；
3. 筛选电量和载荷满足要求的无人机；
4. 按“距离近、剩余电量高、灭火流量大”的顺序排序；
5. 依次选择无人机直到满足资源需求；
6. 检查返航、补给和安全余量；
7. 输出调度方案和决策理由。

后续有时间再将第 4、5 步替换为整数规划、遗传算法或强化学习方法。项目演示阶段，规则清晰、结果稳定比算法复杂度更重要。

## 十五、统一输入输出接口

### 15.1 分析接口

```text
POST /api/analyze
```

输入：

```json
{
  "scene_id": "forest-demo-01",
  "image_path": "uploads/fire.jpg",
  "use_vlm": true,
  "fleet_snapshot": "default"
}
```

输出：

```json
{
  "analysis_id": "analysis-001",
  "fire_assessment": {
    "level": 2,
    "fire_area_m2": 1800,
    "smoke_area_m2": 4200,
    "confidence": 0.91,
    "spread_direction": "西北"
  },
  "environment": {
    "wind_speed": 6.5,
    "wind_direction": "西北",
    "altitude": 320,
    "terrain": "丘陵",
    "nearest_water_distance_m": 800
  },
  "dispatch_plan": {
    "can_control": true,
    "recommended_material": "water",
    "material_amount": 80,
    "tasks": [
      {"drone_id": "drone-1", "task": "reconnaissance"},
      {"drone_id": "drone-2", "task": "firefighting"},
      {"drone_id": "drone-3", "task": "resupply"}
    ],
    "estimated_minutes": 18
  },
  "explanation": "当前为中等火情，西北风可能推动火势向西北扩散。建议无人机二灭火，无人机三前往水源点补给。"
}
```

### 15.2 闭环接口

```text
POST /api/monitor/{analysis_id}
```

输入：

- 新一轮图片或视频帧；
- 当前无人机状态；
- 当前物资库存；
- 距离上一轮监测的时间。

输出：

- 新火情面积；
- 火势增长或减少比例；
- 当前任务效果；
- 下一步动作：`continue`、`resupply`、`reinforce`、`return` 或 `finish`；
- 决策理由。

## 十六、按成员拆分的实际开发任务

### 江月

负责业务定义和项目统筹：

- 确认最终 Demo 场景和展示故事；
- 整理火情等级、无人机角色和参数假设；
- 维护需求文档、Word 长文档和 PDF 方案；
- 确认每个模块是否符合会议中的业务设想；
- 组织联调、报告撰写和视频脚本。

### m（马其瑞）

负责基础框架和主流程：

- 建立 GitHub 仓库和目录结构；
- 搭建 FastAPI 后端和 Vue 前端；
- 定义 Pydantic 数据模型；
- 实现 Agent 工作流和 Tool 注册机制；
- 使用假数据跑通 `/api/analyze`；
- 集成各成员模块并处理统一输出。

### 听日

负责视觉识别模块：

- 确认数据集来源和许可证；
- 获取并测试 Best 权重；
- 跑通 YOLO 火点检测；
- 输出检测框、面积、中心点和置信度；
- 编写视觉模块使用说明。

### 杨涵

负责地理和环境模块：

- 确定固定森林区域；
- 整理地形、海拔、水源和风向数据；
- 设计 `scene.json` 或 GeoJSON；
- 实现环境查询和水源距离计算；
- 为后续航线规划准备地图数据。

### 吉相如

负责 VLM/LLM 模块：

- 测试 Kimi、千问等视觉模型；
- 设计火情解释 Prompt；
- 约束模型输出为 JSON；
- 实现模型适配器；
- 将模型结果交给规则引擎校验，不直接覆盖确定性计算结果。

## 十七、最小 Demo 的验收标准

### 必须完成

- 能上传一张火灾图片；
- 能显示 YOLO 检测结果；
- 能读取固定场景的环境数据；
- 能读取三类无人机状态；
- 能计算火情等级；
- 能计算灭火资源和无人机数量；
- 能生成任务分配结果；
- 能展示智能体的分析说明；
- 能输出完整 JSON 结果；
- 能录制一段从输入到输出的完整视频。

### 有时间再完成

- 视频多帧分析；
- 风向和火势趋势预测；
- 补水和返航状态动画；
- 第二轮闭环监测；
- GIS 地图展示；
- 人群疏散和语音播报。

### 暂不纳入首版

- 真实无人机控制；
- 真实飞行避障；
- 真实消防作业参数；
- 自行训练复杂改进 YOLO；
- 多个独立大模型服务部署；
- 真实传感器和在线气象服务。

## 十八、最终需要完成的文件

```text
项目代码
├─ README.md
├─ requirements.txt
├─ backend/
├─ frontend/
├─ data/
│  ├─ scene.json
│  ├─ fleet.json
│  └─ inventory.json
├─ configs/
│  └─ simulation.yaml
└─ docs/
   ├─ requirements.md
   ├─ architecture.md
   ├─ data-dictionary.md
   ├─ algorithm.md
   └─ demo-script.md

最终提交材料
├─ 森林火灾救援智能体项目方案.pdf
├─ 系统演示视频.mp4
├─ 可运行程序或 GitHub 仓库
└─ 使用说明和交互指南
```

