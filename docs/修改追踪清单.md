# 修改追踪清单（Change Tracker）

> 建立时间：2026-09-04。基线：当日 main（路由拆分后），基线门禁 = `pytest -q` 33 passed + `py_compile` + `npm run build` 全绿。
> 本清单是平台全部修改的唯一登记处；负责人定义见 [CONTRIBUTING.md](../CONTRIBUTING.md) 第 4 节（A=任务链路与规则引擎，B=环境态势与前端，外部=模型协作）。
> 登记与流转规则见文末"追踪规则"；契约级变更必须同时走 CONTRIBUTING 第 6.1 条流程。

## 状态定义

`待办 → 进行中 → 待验收 → 完成`；另有两类特殊状态：`暂缓`（依赖外部资源）、`冻结`（历史存档，禁止修改）。

## 一、已完成（清单建立时回填）

| ID | 完成日期 | 事项 | 负责人 | 验证方式 |
|---|---|---|---|---|
| D0-01 | 2026-09-04 | 开发规范与 API 契约建立（CONTRIBUTING.md、docs/api-contract.md） | A | 文档评审 |
| D0-02 | 2026-09-04 | 五处文档口径修正（README 报告路径、data-dictionary schema_version/调度字段、思路细化接口名、实施规划 Context 与 DispatchPlan 注记、项目方案历史声明） | A | grep 残留检查 |
| D0-03 | 2026-09-04 | `calculate_flp_load` 键名缺陷修复：冻结公式 B_i=10×I×K_fuel×K_wind×K_slope 全因子生效 | A | tests/test_rules.py::test_flp_grid_standard_example |
| D0-04 | 2026-09-04 | 首个方案补齐 `plan_version: 1` | A | tests/test_scenarios.py 场景 3/6 |
| D0-05 | 2026-09-04 | 前端演示兜底升级为 2+4+2 正口径：移除 DR-01/02/03、"18 分钟"单点与 3+3+2 旧数据；时间区间改读 `earliest_minutes/latest_minutes` | B | npm run build |
| D0-06 | 2026-09-04 | 测试补齐：规则算例 / 适配器协议 / 六场景 API 级验收 | A | pytest -q |
| D0-07 | 2026-09-04 | 路由层按域拆分 routes/（A 任务域、B 环境域），消除并行开发共享文件 | A+B | pytest 33 passed |

## 二、开发者 A（任务链路与规则引擎）

| ID | 优先级 | 事项 | 涉及文件 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|---|
| A-1 | P0 | 坐标系统一：`fire_origin` 改为相对坐标（米）并与机群同系，GPS 参考另存 `fire_origin_gps`；视觉 `fire_center` 改用 `latitude/longitude` 键；闭环监测火点原点与调度同源（`result.scene.fire_origin`） | `data/scene.json`、`data/vision_observations.json`、`skills/registry.py`、`pipeline.py`、api-contract §1.3、data-dictionary、CONTRIBUTING §10 | CONTRIBUTING §10、api-contract §1.3 | **完成（2026-09-04）**：坐标契约测试 `test_scene_fixture_coordinates_share_one_relative_frame`、`test_dispatch_and_monitor_share_scene_origin_frame` 通过 | 完成 |
| A-2 | P1 | SQLite 持久化替换内存 AnalysisStore（写穿式：任务、方案版本、事件、资源锁、fleet/inventory 快照；`data/analysis_store.db`，git 忽略，删除即重置演示状态） | `domain/store.py`、tests/test_persistence.py、api-contract §5、README ×2、CONTRIBUTING §12 | task-progress、审计 §八 | **完成（2026-09-04）**：重启恢复测试 `test_store_survives_restart` 等通过；全套件绿 | 完成 |
| A-3 | P1 | 任务事件 SSE 流 `GET /api/tasks/{id}/events/stream`（快照先行→增量推送→keep-alive→终态 done；`once=1` 快照模式） | `routes/task_routes.py`、tests/test_batch3.py、api-contract §5.10 | 审计 §八 | **完成（2026-09-04）**：`test_event_stream_sends_snapshot_then_done` 通过；前端 B-5 已接入 | 完成 |
| A-4 | P1 | 多帧图片序列：`frames` 上传字段 + `analyze_frame_sequence` Tool + 趋势驱动火情重算（主文件=最新一帧），响应 `result.visual_sequence` | `routes/task_routes.py`、`analysis_service.py`、`tools/core.py`、tests/test_batch3.py、api-contract §5.2 | 审计 §六.3 | **完成（2026-09-04）**：`test_multi_frame_upload_returns_visual_sequence` 通过 | 完成 |
| A-4b | P1 | `target_minutes` 硬时限（规则文档 §8.2"先剔除超时方案"）：全部可控候选超时时选最快方案、判不可控并输出 `time_limit` 资源缺口 | `pipeline.py`、tests/test_contracts.py、api-contract §5.1 | 无人机规则 §8.2 | **完成（2026-09-04）**：`test_time_limit_constraint_filters_overtime_plans` 通过 | 完成 |
| A-5 | P2 | 视频抽帧接入主链：OpenCV 可用时 MP4 自动抽帧参与感知 | `tools/core.py`（extract_frames）、`skills/registry.py` | 审计 §六.3 | 无 OpenCV 时保持 `not_available` 不报错 | 暂缓 |
| A-6 | P2 | 多机优化航线与真实能耗模型 | `pipeline.py`、`tools/core.py` | task-progress | 标准算例可复算；不破坏冻结公式测试 | 暂缓 |
| A-7 | P2 | 人群疏散扩展接入（`plan_evacuation_route` 已有 Tool，未挂入主链） | `skills/registry.py` | 思路细化 §6 | 有人分支可选输出疏散路线 | 暂缓 |

### 算法与地图实景专项（2026-09-04 登记执行）

| ID | 优先级 | 事项 | 涉及文件 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|---|
| SK-1 | P0 | 默认坐标改到真实紫金山山体：DEM 实测主峰 438m @ (32.0725N, 118.8415E)，替换原平原默认坐标 (32.1256, 118.9585)；terrain/environment 服务、前端初始坐标、scene.json `fire_origin_gps` 全部对齐 | `terrain_service.py`、`tools/environment.py`、`App.vue`、`data/scene.json` | 用户需求"紫金山实景"；api-contract §1.3、§3.2 | 等高线返回真实山体海拔（≥300m 档）；新契约测试覆盖 | **完成（2026-09-04）**：默认坐标改为紫金山主峰 (32.0725, 118.8415)；实测等高线 40–320m 真实山形；`test_terrain_contours_on_real_zijin_mountain` 通过；真实环境调用返回海拔 438m + 紫霞湖等真实水源 GPS |
| SK-2 | P1 | Skill 链真实化：CandidateGenerationSkill 透出 `fire_origin`；RoutePlanningSkill 目标改用真实火点（去硬编码 800,0）；EvacuationSkill 用 `plan_evacuation_route` 实现确定性疏散路线 | `skills/registry.py` | 思路细化 §6、§10；审计 §三 | 全 15 Skill 链 smoke 测试通过；疏散路线有人分支可产出 | **完成（2026-09-04）**：`test_full_skill_chain_uses_real_fire_origin`（12 步链完整、航线目标=真实火点）与 `test_evacuation_skill_plans_route_around_fire` 通过 |
| SK-3 | P1 | 前端地图实景对应：火点标记由任务数据定位（fire_origin 相对坐标/fire_center GPS），新增米制比例尺 | `frontend/src/App.vue`、`workbench.css` | 用户需求"标注实际位置" | 火点、水源、道路、等高线在同一坐标框架；build 通过 | **完成（2026-09-04）**：火点标记由 `scene.fire_origin` 驱动并标注 GPS；新增米制比例尺；npm run build 通过 |
| SK-4 | P1 | 全量 Tool/Skill 健康测试：47 个注册 Tool 逐一执行冒烟、15 个 Skill 链路冒烟 | `tests/test_skills_tools.py` | 用户需求"处理好 skill 和 tool" | 全部 Tool 信封合法、核心 Tool ok:true；Skill 链全绿 | **完成（2026-09-04）**：52 个注册 Tool 全量执行成功（`test_core_tools_all_succeed`）；Skill 链冒烟通过；套件 46 passed |

### Playwright E2E 十轮测试-修复（2026-09-04 执行，e2e/round1-9.py）

| 轮次 | 覆盖 | 结果 | 修复的问题 |
|---|---|---|---|
| R1 | 地图实景：真实 DEM 等高线、海拔标签、火点 GPS、比例尺 | 9/9 | 火点标注改用场景火点 GPS；onMounted 并发时序竞争测试修正 |
| R2 | 研判流程：上传→研判→待确认→可控/不可控双场景 | 12/12 | **acceptFile 收到单个 File 不可迭代的回归**；**environmentForPayload 未定义导致研判必然失败**；状态中文映射补齐（待确认/已终止等） |
| R3 | 审批闭环：批准→执行→反馈轮次→终止 | 7/7 | 前端审批失败透出后端具体原因；E2E 轮次自清理资源锁 |
| R4 | 调整约束：出动上限/禁用设备→v2 版本→驳回 | 6/6 | **adjust 返回 plan 信封导致 analysisId 丢失**，改为调整后重拉任务状态 |
| R5 | SSE 事件流 + 报告在线查看 | 5/5 | 验证 text/event-stream 200、审批事件实时上屏、报告 JSON 31KB 完整 |
| R6 | SQLite 重启恢复（API+UI） | 4/4 | 历史任务恢复后方案摘要不显示（v-if 过严）修复；两次重启 26/36 任务全量恢复 |
| R7 | 多帧序列上传 | 4/4 | 断言改走后端 visual_sequence（本地日志被事件覆盖属既有设计） |
| R8 | 禁用设备约束 | 3/3 | E1/E2 禁用后任务分工 chips 正确排除 |
| R9 | 硬时限约束 | 5/5 | **pipeline 违反契约：超时不可控仍输出时间窗口**，修复为 window=None 并两次重启复验 |
| R10 | 总回归：R1/R2/R3/R6 重跑 + pytest + build | 全绿 | 9+12+7+4 PASS；pytest 46 passed；npm build 通过 |

### 前端数据流与显示适配专项（2026-09-04 登记，B 域自查）

| ID | 优先级 | 事项 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| FE-1 | P0 | plan_id/plan_version 数据流错位：`result.dispatch_plan` 不含 plan_id/plan_version（仅 plan_versions 有），摘要恒显"当前方案/空版本"，且审批不带 plan_id 失去过期方案保护 | api-contract §7、§5.5 | 摘要显示真实 plan_id 与版本号；approve 携带精确 plan_id | **完成（2026-09-04）**：摘要显示真实 plan_id（plan-0384458e63）与版本号 v1/v2；approve 携带精确 plan_id，E2E R2/R3 回归通过 |
| FE-2 | P1 | SSE 快照与 loadEvents 竞态导致日志重复条目（历史任务恢复路径） | E2E R6 发现 | 恢复任务后日志无重复 | **完成（2026-09-04）**：SSE 事件按键去重；R6 历史恢复 43 条任务日志无重复 |
| FE-3 | P1 | metrics 硬编码假变化率 '+12.4%'/'+8.1%' | 冻结口径"数字有来源" | 变化率从真实轮次 before/after 计算 | **完成（2026-09-04）**：火焰面积变化率由真实轮次计算（实测 -16.7%）；烟雾显示真实增长率 |
| FE-4 | P1 | hero"预计处置时间"不可控时显示空白（estimated_minutes=null） | api-contract §7 | 显示 '—' | **完成（2026-09-04）**：不可控时显示 '—'（实测可控时显示真实 76 MIN） |
| FE-5 | P2 | 场景名不一致：scene.json"青龙山演示林区" vs 前端"紫金山侦察区"（数据已固定紫金山） | SK-1 | 统一为紫金山 | **完成（2026-09-04）**：scene.json/api-contract/前端统一为「紫金山演示林区」 |
| FE-6 | P2 | 监测结果把 next_inventory 整对象 JSON.stringify 倾倒，显示脏 | 显示适配 | 只显示水剂/模块/电池关键量 | **完成（2026-09-04）**：库存显示关键量（水/W20/C6/电池），不再 JSON 倾倒 |

### 水源标注与前端优化专项（2026-09-04 登记，B 域）

| ID | 优先级 | 事项 | 验收标准 | 状态 |
|---|---|---|---|---|
| FE-7 | P1 | 水源标注完整化：地图标记按水体类型着色、首选水源高亮、林区态势新增「水源标注」清单面板（名称/类型/距离/坐标，按距离排序）、环境面板水源行补首选水源 | 面板与地图标记数量一致；E2E 断言 | **完成（2026-09-04）**：**完成（2026-09-04）**：E2E 实测——水源面板 2 条（溪流 786m、★紫霞湖水库 1166m 带 GPS）与地图标记 2=2 一一对应，首选高亮生效，水体类型着色（水库/湖/河/塘）；环境面板新增首选水源行 |
| FE-8 | P2 | 前端优化：无人机地图标记带 SOC、标记悬停置顶 | build 通过、E2E 无控制台错误 | **完成（2026-09-04）**：无人机地图标记带 SOC（R1 · SOC 92%）；标记悬停置顶放大；npm build 通过、控制台无错误 |

### 林区态势战术化升级（参考 firepatrol-agents 工作台，2026-09-04 登记，B 域）

| ID | 优先级 | 事项 | 验收标准 | 状态 |
|---|---|---|---|---|
| FE-9 | P1 | 战术地图升级：①火情范围圈（按 fire_area_m2 等效半径绘制于火点）②疏散路线叠加（skill_chain.evacuation 路径网格→相对坐标折线+出口标记）③「任务部署」面板（每架 UAV 状态/SOC 条/当前任务）④火情演化 sparkline（逐轮 FLP 曲线） | E2E：面板 8 机、有人分支显示疏散折线、圈随监测收缩 | **完成（2026-09-04）**：**完成（2026-09-04）**：E2E 实测——部署面板 8 机（状态/SOC 条/当前任务/出动高亮）；火情范围圈随监测收缩；有人分支疏散折线+出口标记渲染、无人分支不显示；演化曲线逐轮 FLP（单轮显示提示文案） |
| FE-10 | P2 | 交互细节：Esc 关闭报告查看器；图例补疏散路线项 | build 通过、无控制台错误 | **完成（2026-09-04）**：Esc 关闭报告查看器实测通过；图例补疏散路线项 |
| FE-13 | P0 | 修复 TacticalMap 组件化遗留的模板错误：`tactical-map-wrap` 缺失闭合标签，`map-stage` 无法闭合，`npm run build` 报 App.vue "Element is missing end tag"（工作区一度处于不可构建状态） | build 通过 | **完成（2026-09-04）**：在 `map-side-rail` 之前补 `</div>` 闭合 tactical-map-wrap（侧栏回归 map-stage 的 flex 兄弟节点，与 workbench.css .map-stage/.map-side-rail 规则一致）；npm run build 通过 |
| FE-14 | P0 | 高德真实地图激活：填入用户申请的 Web端 Key + securityJsCode（frontend/.env，git 忽略）；首次真实联调修复 AMap 分支（此前从未跑通，恒走 SVG 回退）；验证真实水源 OSM 落点精准（GCJ-02 转换）、取水路线、图层开关、联动；scene.json 演示水源补 latitude/longitude（契约 §1.3/§3.1 同步 + 测试），demo 模式标注同样精准 | E2E round10：卫星底图加载、真实水源标注、取水虚线、联动与控制台零错误；pytest/build 全绿 | **完成（2026-09-04）**：①.env 配 Key（gitignore 覆盖）。②Overpass 主源被墙实测（SSL EOF），`environment_service.overpass_query` 加 mail.ru 镜像回退（实测含紫霞湖/黄马水库等完整中国数据，主源快速失败 1 次即切换）；`tools/environment.py` partial 结果不写缓存（空水源不再占 5min TTL）。③联调修复 3 个真 bug：marker-detail/图层开关/map-coords/map-source 四浮层误嵌 SVG v-else 分支（高德模式全消失，移回 tactical-map-wrap 层两模式共用）；TacticalMap overlay meta 缺 label/type（清单联动详情卡显示原始 id 且水源被当无人机构建 rows）；FE-13 遗留——R1 断言 SVG 分支元素在 AMap 模式全挂，R1 改分支自适应。④scene.json 北侧蓄水池补 GPS（32.0701,118.8432，与 distance_m=150 自洽）+ api-contract §1.3/§3.1（修正 §3.1 示例残留旧口径 x/y 塞经纬度）+ data-dictionary + `test_demo_water_source_carries_display_gps`。⑤E2E：新增 round10（13/13 PASS：真实水源 20 标注、紫霞湖首选高亮、取水路线 1166m、光标经纬度、图层开关、清单联动、演示模式 GPS 标注）；R2/R3-R9 的 `FLP：135` 死值断言与实时风耦合（Open-Meteo 晚间南风 2.52m/s → FLP 30，晨间标定失效），改方案就绪/状态切换断言；R9 时限 30→1 分钟（任何场景必超时）；R6 历史任务选择器 button→tab（FE-12 语义化）。pytest 51 passed；npm build 通过；R1-R10 全绿 |

### 工作台视觉重构与后端性能优化（2026-09-05 登记，用户反馈"前端太丑"；impeccable product register 全清单执行）

| ID | 优先级 | 事项 | 验收标准 | 状态 |
|---|---|---|---|---|
| FE-15 | P0 | 视觉重构：①字体收敛 Manrope+DM Mono（数据 mono ≥12px，正文 14px）②真 CSS token（bg/surface/line/ink/accent/语义色）清除一次性 hex ③去全部英文 eyebrow kicker → 统一面板头 ④hero 删假 SVG 地图改排版状态带 ⑤舰队页同款卡片网格→分组花名册表（侦察/灭火/支援）⑥KPI 四卡→统计带 ⑦地图页工作台化（工具栏行+画布+停靠面板列+底部状态栏，用户修订）⑧地图标记/等高线减负 ⑨去 side-stripe 等禁手 | E2E 选择器全保留（.plan-summary/.task-badge/.map-legend/.map-coords/.map-source/.map-side-rail/.marker-detail/.tmap-*/role=tab）；pytest/build/R1-R10 全绿；三视图改前后截图对比 | **完成（2026-09-05）**：style.css/workbench.css 全量重写（token 化，去 Barlow Condensed 收敛 2 字体）；hero 状态带（结论 23px 随 ok/bad 着色，hero-footer DOM 契约保留）；fleetGroups 分组花名册；地图页 map-toolbar/map-stage/map-statusbar 三段式；TacticalMap 无人机环半径 520m+、等高线 0.4/0.8 不透明度、新增 applyWaterZoom 缩放联动（zoom<15 仅显示首选水源，renderAll/applyVisibility/zoomend 三路挂载）；round10 适配（低倍仅首选 + 双击放大展开，滚轮粒度不可靠弃用）；改动前后截图 redesign_*.png 对比存档。pytest 51 passed；build 通过；R1-R10 全绿 |
| BE-1 | P1 | 后端性能：①启动后台线程预热紫金山默认坐标环境缓存（演示冷启动不等 Overpass 30-60s）②环境请求 single-flight（并发相同 key 合并为一次抓取）③Overpass/Open-Meteo 分段超时（连接 3s） | 冷启动首请求 <2s（缓存命中）；并发请求只出一路网络调用；pytest 全绿 | **完成（2026-09-05）**：main.py lifespan 启动 environment-warmup daemon 线程（失败静默回落正常链路）；tools/environment.py single-flight（_inflight Event 表，leader 抓取 follower 等 120s 后读缓存，未落地自行抓取）；overpass_query timeout=(3,25)、Open-Meteo (3,15)。实测：启动 75s 后首个环境请求 0.004s 返回 20 水源 status=ok |
| FE-16 | P1 | 地理范围与坐标精度（用户需求"范围再扩大、更精确"）：①水源/道路检索半径默认 3000→5000m（契约级：api-contract §3.1/§8 同步，schemas/routes/tool/skills 默认值全链对齐，前端环境查询与研判上传同步 5000）②等高线请求 radius_deg 0.04→0.05 + max_points 180→240（覆盖更大、采样更细，§3.2 接口默认不变）③界面经纬度显示统一 6 位小数（火点标签/水源行/详情卡/光标读数/环境面板，原来 4-5 位混用） | 契约文档与实现同提交；pytest/build 全绿；5000m 半径真实请求返回正常水源；等高线覆盖扩大且来源正常 | **完成（2026-09-05）**：9 文件 18 处半径链路对齐 5000；等高线 38 条 feature（bounds 0.05°）；实测 5000m 半径 nearest-20 排满（紫金山周边水体密集，最远 2218m）；坐标 6 位小数全站生效（R10 详情卡实测 118.849085°E）；pytest 51/build/R1+R10 冒烟全绿 |
| FE-17 | P0 | 紫霞湖无人机基地 + 出动动画 + 补水/电量推演（用户需求：基地设在紫霞湖、点击运行各模块出发、按后端仿真执行含补水/电量）：①fleet.json 8 架 position 迁紫霞湖停机坪（-254,-671 基准+散布），inventory 水源改名紫霞湖水库 ②契约测试 fleet 坐标边界 [0,500]→[-1500,1500]（同框架意图不变）③前端 mission 推演模块：批准即出动动画（相位 flying→working→returning→servicing→charging 对齐 simulate_monitor 状态机，rAF + marker.setPosition 逐帧移动，相位徽章+SOC 条）④自动推演钟（executing 期间自动 POST 轮次，后端权威校准，replan/终态停止）⑤部署面板相位列 ⑥e2e/round11.py 动画专项 | E2E R1-R11 全绿 + pytest/build 全绿；动画启动后标记位置随时间变化、自动推演≥2 轮 SOC 下降/补水相位出现、终止即停 | 执行中（2026-09-05） |

### 可交互效果专项（2026-09-04 登记，B 域）

| ID | 优先级 | 事项 | 验收标准 | 状态 |
|---|---|---|---|---|
| FE-11 | P1 | 可交互效果：①图例变图层开关（六图层显隐切换）②地图标记点击详情卡（无人机 SOC/任务、水源类型/距离、火点 FLP）③面板↔地图联动（悬停部署行高亮标记、点击行脉冲定位）④过渡动画（面板入场/按钮按压/reduced-motion） | E2E：图层切换数量变化、详情卡出现、悬停联动 class、无控制台错误 | **完成（2026-09-04）**：E2E 全过——图层开关（无人机 0↔8、等高线 0↔N）、无人机/水源详情卡、Esc 关闭、悬停联动、点击行脉冲定位、控制台零错误。附带修复：模板模板可选链兼容（deployRowClass 提为方法）、地图拖拽 pointer capture 排除交互元素、浮层 z-index 统一（图例曾被 SVG 层遮挡）、E2E close() 自动清理 |
| FE-12 | P1 | 全前端系统性优化（impeccable product register 全清单）：键盘焦点指示器全局统一、按钮禁用态、muted 正文对比度 ≥4.5:1、面板细滚动条、触摸目标加大、tab pills role/aria-selected 语义、历史任务/日志空态教学文案、机群加载骨架屏、研判失败 notice 重试按钮、全局 prefers-reduced-motion 兜底 | E2E：tab 语义 3/3、键盘 Tab 焦点可见、控制台零错误、build/pytest 全绿 | **完成（2026-09-04）** |

### 规则完成度审计与补齐（2026-09-04 登记，对照 docs/无人机子群与参数规则1.md）

> 审计方法：53 个注册 Tool 逐一核对主链调用点（21 个显式 execute + 7 个 core 内部 helper + pipeline 直接函数调用）；规则文档逐条对照。
> 判定：核心规则链（FLP/SOC/J 评分/硬约束/离散仿真/闭环）已完整；以下 4 条规则约束未真正生效，补齐。

| ID | 优先级 | 事项 | 涉及文件 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|---|
| G-1 | P0 | 35% 新任务 SOC 底线生效：候选过滤从固定 25 改用配置 `new_task_floor_soc_percent`（25 仅作返航阈值） | `pipeline.py`、api-contract §8 | 规则 V1 §4.2（SOC<35% 不得新接远程任务） | 低 SOC 机不进候选集的测试通过 | **完成（2026-09-04）**：候选过滤改用 new_task_floor_soc_percent(35)；test_new_task_soc_floor_excludes_low_battery_units 通过 |
| G-2 | P0 | 就地取水六条件接入调度：`water_source_plan` 由硬编码 base 改为 `select_water_source` 真实评估 | `pipeline.py`、`tools/core.py` | 规则 V1 §5.3、审计 §二.4"仅有 Tool 不等于实际使用" | water_source_plan 携带真实评估 reason；测试覆盖 | **完成（2026-09-04）**：water_source_plan 由 select_water_source 六条件真实评估；test_water_plan_evaluated_via_six_conditions 通过 |
| G-3 | P1 | SOC<15% 应急回收标记：闭环监测输出 `emergency_units`（25% 返航兜底不变） | `pipeline.py`、api-contract | 规则 V1 §4.2（SOC<15% 应急） | 低电量算例 emergency_units 非空测试通过 | **完成（2026-09-04）**：monitor 输出 emergency_units/emergency_soc_percent；test_monitor_flags_emergency_units_below_15_percent 通过 |
| G-4 | P1 | 有人分支疏散进入核心链：people=confirmed 时 evacuation Skill 结果进 skill_chain 并随任务返回；前端显示疏散路线摘要 | `skills/orchestrator.py`、`App.vue` | 思路细化 §6、规则 V1 §9 | confirmed 链含 evacuation 且路线 found；前端显示 | **完成（2026-09-04）**：confirmed 核心链追加 evacuation（12 步+疏散）；E2E 验证有人显示疏散摘要/无人不显示；test_confirmed_people_chain_includes_evacuation 通过 |

### 审计连带发现并修复（同日）

| ID | 级别 | 问题 | 修复 | 验证 |
|---|---|---|---|---|
| X-1 | P0 | 主任务分析一直使用旧版 6 步演示 Skill 注册表（fire_analysis.py），新 12 步核心链从未在主任务运行 | task_routes 改用 skills/registry.py 新注册表 | E2E chain keys 含 candidate_generation 等 14 步 |
| X-2 | P0 | `analyze/upload` 所有表单标量（scene_id/latitude/longitude/environment_mode/use_vlm/people_status）因缺 `Form(...)` 绑定而从 query 读取——**坐标、环境模式、人员状态从未送达研判** | 全部改显式 Form(...) | curl/E2E people_status=confirmed → branch confirmed → evacuation 入链 |
| X-3 | P0 | CandidateGenerationSkill 的 fire 字典缺 fire_load_flp，调度退化成 面积/180 的错误负荷（小火 1.44 FLP 一轮"灭完"致任务误判完成） | Skill 内用 build_fire_grid 同源公式计算 FLP | 契约测试全过；dispatch/assessment FLP 单一来源化（run_demo_analysis 回写） |
| X-4 | P1 | project-status 依赖新注册表已移除的 fire_analysis Skill → 500 | 直接统计感知 Skill 的 ToolRegistry（53 tools/15 skills） | E2E 控制台无错误 |

## 三、开发者 B（环境态势与前端）

| ID | 优先级 | 事项 | 涉及文件 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|---|
| B-1 | P0 | 紫金山坐标基准标注（配合 A-1）：GPS 基准入 `fire_origin_gps`，前端地图展示口径不变 | `data/scene.json` 标注、api-contract §1.3 | api-contract §1.3 | **完成（2026-09-04）**：契约与字典已同步 | 完成 |
| B-2 | P1 | 驳回/终止原因必填：原因输入框 + 空值拦截，reason 进入任务事件与报告 | `frontend/src/App.vue` | 审计 §九 | **完成（2026-09-04）**：构建通过；空原因提交被前端拦截并提示 | 完成（浏览器人工复核待 J-1） |
| B-3 | P1 | 方案调整完整表单：出动上限、目标时限（分钟）、禁用设备 E1–E4 勾选 | `frontend/src/App.vue`、`workbench.css` | 审计 §九、实施规划 | **完成（2026-09-04）**：约束经 `adjust` 生效，后端 `target_minutes` 测试通过；构建通过 | 完成 |
| B-4 | P1 | 多帧序列上传 UI：文件多选、最新一帧为主文件、序列帧计数提示 | `frontend/src/App.vue` | 审计 §六.3 | **完成（2026-09-04）**：构建通过；后端序列契约测试通过 | 完成（浏览器人工复核待 J-1） |
| B-5 | P1 | SSE 前端接入：任务创建/恢复时自动订阅事件流，实时上屏，终态 done 自动关闭，断流降级轮询不受影响 | `frontend/src/App.vue` | 与 A-3 配对 | **完成（2026-09-04）**：构建通过；EventSource 生命周期管理完成 | 完成（浏览器人工复核待 J-1） |
| B-6 | P2 | 地图道路几何与风险路线显示（真实道路线，替换示意线） | `environment_service.py`、`terrain_service.py`、`App.vue` | 审计 P1-6 | 道路 GeoJSON 上图；仍标注"局部态势图，非导航地图" | 待办 |
| B-7 | P2 | 在线气象/实时水源增强（缓存失效与来源展示补齐） | `environment_service.py`、`environment_cache.py` | task-progress | stale 标注与来源展示完整 | 暂缓 |
| B-8 | P2 | App.vue 组件化拆分（upload/decision/map/history 分文件） | `frontend/src/` | 工程健康度 | 拆分后 build 通过、页面功能回归 | 暂缓 |

## 四、双人共同项

| ID | 优先级 | 事项 | 负责人 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|---|
| J-1 | P0 | 浏览器端六场景人工验收（场景清单见 CONTRIBUTING §9；本轮新增能力一并复核：SSE 实时上屏、多帧上传、原因必填、时限缺口展示） | A+B | task-progress 交付验收口径 | 六场景截图/录屏归档，结果记入 task-progress | 待办（演示录制前执行，需人工） |
| J-2 | P0 | A-1/B-1 坐标统一联调回归 | A+B | A-1、B-1 | **完成（2026-09-04）**：全套件 41 passed 含坐标与六场景测试 | 完成 |

## 五、外部协作（不修改本仓库代码）

| ID | 事项 | 负责人 | 依据 | 验收标准 | 状态 |
|---|---|---|---|---|---|
| E-1 | YOLO/PWM-YOLO 独立检测服务交付（权重、启动方式、字段对齐） | 听日 | api-contract §9 | 平台侧 curl 冒烟 + fixture 字段对齐 + 契约测试通过 | 待办（外部） |
| E-2 | VLM 独立解释服务交付 | 吉相如 | api-contract §10 | 输出 JSON 可解析、不覆盖规则数字、契约测试通过 | 待办（外部） |
| E-3 | 交付材料：项目方案 PDF、系统演示视频、可运行程序与使用说明（截止 2026-09-30） | 江月统筹，A/B 提供演示环境 | 项目方案 §一、§十八 | 按竞赛提交清单核对 | 待办（外部） |

## 六、冻结项（禁止修改）

| 对象 | 原因 |
|---|---|
| `森林火灾救援智能体项目方案.md` | 历史立项存档，只允许顶部声明（已有） |
| `docs/实现差异审计.md` | 2026-09-03 审计快照，不随代码更新 |
| 冻结业务口径（2+4+2、FLP 公式、W20/C6、SOC、时间区间、审批门禁） | CONTRIBUTING §2，变更需双人确认并走契约流程 |

## 追踪规则

1. **登记时机**：任何非琐碎修改开工前在本清单登记（ID、负责人、涉及文件、依据、验收标准）；琐碎修改（错别字、注释）可事后补记。
2. **状态流转**：完成一项立即更新状态并填入验证方式（测试名 / 构建结果 / 人工验收记录）；禁止只改代码不改清单。
3. **配对项**：标注"配对"的条目（A-3↔B-5、A-4↔B-4、A-1↔B-1↔J-2）由先开工一方在群里声明，另一方确认后再动工。
4. **契约级变更**（新增/修改接口字段、数据文件、冻结口径）：先走 CONTRIBUTING §6.1（文档→测试→实现同提交），再在本清单登记。
5. **优先级含义**：P0=演示/交付阻塞；P1=完整演示应完成；P2=时间允许再做。9 月 30 日截止前，P0 全清、P1 尽量清、P2 可豁免。
6. **每周核对**：每次联调前过一遍本清单，关闭已完成项、复评优先级。
