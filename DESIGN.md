# DESIGN.md

## Theme

浅色专业指挥台。背景 `#f2f4f6`（冷灰），主面板白 `#ffffff`，次级面 `#f6f8f9`，侧栏深色 `#151d26`。
状态色：蓝 `#2563eb`（信息/主操作/选中）、橙红 `#d9541e`（火情/警示）、绿 `#0e8a5f`（正常/成功）、紫 `#6d4fa1`（方案/轮次/等高线语义）。
全部色彩以 CSS token 收敛于 style.css `:root`（bg/surface/line/ink×3/accent/语义色/shadow 两级），禁止一次性 hex。

## Typography

- UI/标题：Manrope（400–800），字重分层制造层级；h1 25px/800 → 面板标题 17px/700 → 正文 13.5–14px → chips/标签 12.5px
- 数据/坐标/日志：DM Mono（≥12px，投影可读下限）；字号比例 ~1.2，固定 px 尺度不用 clamp
- 全部字体经 Google Fonts 引入，离线回落系统 sans / ui-monospace

## Components

- 面板：白底、1px `--line`、12px 圆角、20px 内边距、两级阴影；面板头 = 标题（17px/700）+ 右侧状态徽章/操作，**不使用英文 eyebrow kicker**
- Hero 状态带：处置结论为最大元素（23px/800，绿/橙红随 ok/bad 状态），次级为 KPI 数据列
- KPI 统计带：单条面板内 4 格（分隔线切分），大数字 26px/800 + 来源小注，不做卡片阵列
- 舰队：分组花名册表（侦察/灭火/支援组头 + 密集行），不用同款卡片网格
- 按钮：primary（蓝底白字 13.5px/700）、outline（白底灰边）、danger（火情红）；控件高度统一 ~34px
- 状态徽章：软底色 + 深色 mono 文字（pill 形）
- 禁手：side-stripe 边框、渐变文字、玻璃拟态默认、每节 eyebrow、同款卡片网格

## Map（工作台版式）

- 结构：工具栏行（标题 + 图层开关 chips + GPS 徽章）→ 中央画布（高德卫星/SVG 回退）→ 右侧停靠面板列（水源/部署/演化/地形）→ 底部状态栏（等高线来源/等高距/火点基准）
- 标记：暗色玻璃底高可读（火点 pill、水源卡、无人机圆徽）；低倍视图仅显示首选水源，放大展开全部（缩放联动减负）
- 等高线：暖色低不透明度（0.4/主 0.8）叠加于卫星影像，避免压盖

## Layout

- 左侧固定侧栏（品牌/场景卡/导航/操作员），右侧主区（header + 工具栏 + 视图）
- 指挥中枢：2 列 grid（1.65fr/1fr），KPI 统计带横贯
- 态势地图：工作台版式，画布高 `calc(100vh - 318px)`；1000px 以下单列，700px 以下侧栏折叠为顶部导航
